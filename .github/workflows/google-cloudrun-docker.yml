name: GCP

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Setup Gcloud Account
    runs-on: ubuntu-latest
    environment: hw-dev
    env:
      IMAGE_NAME: gcr.io/${{ secrets.GCP_PROJECT_ID }}/fastapi-car-price-pred:latest
    steps:
      # Git checkout
      - name: Checkout
        uses: actions/checkout@v2

      # Login to Google Cloud Platform
      - name: Google Auth
        id: auth
        uses: 'google-github-actions/auth@v0'
        with:
          service_account_key: ${{ secrets.GCP_CREDENTIALS }}
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      # Gcloud configure docker
      - name: Configure Docker
        run: gcloud auth configure-docker --quiet

      # Build image
      - name: Build Docker image
        run: docker build -t $IMAGE_NAME .

      # Push image to registry
      - name: Push Docker image
        run: docker push $IMAGE_NAME

      # Deploy docker image
      - name: Deploy Docker image
        run: gcloud run deploy fastapi-car-price-pred --image $IMAGE_NAME --region europe-west1 --memory 128Mi --min-instances 0 --max-instances 1 --platform managed --port 80 --allow-unauthenticated